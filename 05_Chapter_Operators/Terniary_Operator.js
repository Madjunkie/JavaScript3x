// either this or that

let age = 20;

let result = (age >= 18) ? "Adult" : "Minor";

console.log(result); // Output: Adult

// condition ? true : false

// write the examples below
let score = 85;
let grade = (score >= 80) ? "Pass" : "Fail";
console.log(grade); // Output: Pass

